# Multi-Agent 리서치 & 에이전트 자동 생성 플랫폼 (Synapse)

> 주제를 던지면 AI 에이전트들이 **스스로 검색·크롤링·분석·집필**해 리포트를 만들고,
> "이런 일을 하는 에이전트를 만들어줘"라고 말하면 **실행 가능한 에이전트(도구 코드)를 자동으로 생성·검증·배포**하는 LangGraph 기반 Multi-Agent 플랫폼.
> 리서치 자동화(Research Supervisor)와 **"에이전트를 만드는 에이전트"(Meta-Agent)** 를 하나의 시스템으로 통합했습니다.

---

## 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | Multi-Agent 리서치 & 에이전트 자동 생성 플랫폼 (Synapse) |
| 프로젝트 기간 | 2025.12 ~ 진행 중 (개인 프로젝트) |
| 개발 인원 | 1명 (기획·백엔드·프론트 전담) |
| 담당 업무 | RAG·데이터 파이프라인(적응형 청킹·청크 중요도 계산·쿼리 재작성·하이브리드 검색) 설계·구현, LangGraph 멀티에이전트/오케스트레이션, 아키텍처 설계, 프론트엔드 |
| 핵심 주제 | 자율 리서치 에이전트 + 자연어 기반 에이전트 자동 생성(Meta-Agent) |
| 기술 스택 | Python, FastAPI, LangGraph, LangChain, Anthropic(Claude), Milvus, Neo4j, React 18, Vite, TypeScript |
| 관련 Repository | [Synapse](https://github.com/GiJeongCho/Synapse) |

---

## 프로젝트 배경 & 문제 정의

리서치 업무는 "검색 → 자료 수집 → 정리 → 분석 → 문서화"로 이어지는 **반복적이지만 맥락 판단이 필요한** 작업입니다. 단일 LLM 호출로는 이 전 과정을 안정적으로 수행하기 어렵고, 매번 사람이 프롬프트를 짜야 합니다.

또한 실무에서는 "매일 아침 특정 주제 뉴스를 요약해 메일로 보내줘" 같은 **정형화된 자동화 요구**가 계속 생기는데, 그때마다 개발자가 스크립트를 새로 짜는 것은 비효율적입니다.

이 프로젝트는 다음 3가지 문제를 해결하는 것을 목표로 했습니다.

1. **리서치를 어떻게 자율화하는가** → Supervisor가 상황을 보고 다음 에이전트를 스스로 선택하는 LangGraph 멀티에이전트 오케스트레이션
2. **자동화 요구를 매번 코딩 없이 처리할 수 있는가** → 자연어 요구사항을 받아 **실행 가능한 에이전트·도구(Python)를 LLM이 생성·검증·배포**하는 Meta-Agent
3. **LLM이 만든 코드를 어떻게 신뢰하는가** → 생성 규칙 → 런타임 방어 → 자가개선 → 하네스 → 도구 청소부로 이어지는 **다층 안전망**

---

## 시스템 아키텍처

```
[사용자]  "○○ 주제 리서치" / "△△ 하는 에이전트 만들어줘"
   │
   ▼
[Frontend]  React 18 + Vite + TypeScript (SPA)
   │  ReactFlow 워크플로 시각화 · Neo4j 그래프 뷰 · 에이전트 생성/관리/스케줄 화면
   ▼
[Backend]  FastAPI
   │
   ├── ① Research Supervisor (LangGraph StateGraph)
   │     supervisor ⇄ {search · crawl · graph · analyst · writer}  루프
   │     └ LLM이 JSON으로 next_agent 라우팅, max_iteration 가드
   │
   ├── ② Meta-Agent (에이전트를 만드는 에이전트)
   │     Requirements → Tool Retriever → Provisioner(코드 생성) → Evaluator
   │     └ Meta-Supervisor: Registry 재사용 → Solo / Dual(Builder↔Critic 쌍방 진화)
   │
   ├── ③ RAG 데이터 파이프라인
   │     [인제스트] 문서 타입 분류 → 적응형 청킹(3종) → 5지표 품질평가
   │                → 크기 정규화 → 중요도 스코어링(color_intensity) → 임베딩 → Milvus
   │     [검색]     쿼리 재작성 → Vector + BM25 + Graph(Neo4j) → RRF Fusion → Rerank
   │
   ├── ④ MCP 도구 런타임 : subprocess 격리 실행 · env 주입 · 도구 청소부
   └── ⑤ 스케줄러 : APScheduler(cron) → 에이전트 자동 실행 → 메일 발송

   ├── Milvus Lite   : 문서 청크 · Agent Registry · MCP 도구 명세 (파일 기반)
   ├── Neo4j         : 문서 지식 그래프 (docker-compose)
   └── 외부 API      : Embedding / Rerank(Qwen3), Anthropic LLM, SMTP

배포: FastAPI(:2004) + Vite(:2003) + Neo4j(Docker Compose)
```

---

## 핵심 기능 & 기여

> 이 프로젝트에서 제가 가장 깊게 파고든 영역은 **RAG·데이터 파이프라인**입니다. "문서를 어떻게 자를지 전략을 자동 선택(청킹)"하고, "청크별 중요도를 계산"하고, "LLM으로 검색 쿼리를 재작성"하는 **검색 품질을 좌우하는 데이터 처리 레이어**를 직접 설계·구현했습니다. (1~4번) 그 위에 멀티에이전트·Meta-Agent 오케스트레이션을 얹었습니다. (5~8번)

### ⭐ 1. Adaptive Chunking — 문서 타입별 청킹 전략 자동 채택 (핵심 차별점 ①)

RAG 품질은 LLM보다 **"문서를 어떻게 자르느냐(청킹)"** 에 더 크게 좌우됩니다. "모든 문서에 하나의 청킹 전략은 없다"(LREC 2026 논문)는 문제의식으로, **문서 특성을 분류해 문서마다 최적 청킹 전략을 자동 채택하는 적응형 전처리 파이프라인**을 구현했습니다.

- **① 문서 타입 분류 → 청커 라우팅 (`classifier.py`)**: 정규식 휴리스틱으로 `논문 / 법령 / 뉴스 / 보고서` 등을 빠르게 판별하고(예: `arxiv ID`·`abstract`·`references`, 한국어 `제N조`·`시행령`, `기자`·`특파원`·날짜 패턴), **모호하면 LLM으로 폴백** → **문서 타입에 맞는 청킹 전략을 자동 선택**해 라우팅
- **② 3종 청커 (`chunkers/`) — 문서 구조에 따라 다른 전략 적용**
  - **LLM Regex Splitter**: 문서 앞부분(8K 토큰) 샘플을 LLM에 주고 **문서 전용 정규식 하나**를 생성 → `re.split`으로 전체에 결정적 적용 (구조가 뚜렷한 논문·규정에 강함)
  - **Split-then-Merge**: 2-pass(재귀 분할 → 인접 조각 탐욕 병합). **한국어 조문 경계(`제N조`)와 영어 논문 섹션 제목(Abstract/Method/…)은 병합하지 않고 청크 경계로 보존**
  - **Recursive**: 구분자 계층 재귀 분할 + 오버랩
- **③ 5가지 내재적 지표로 청크 품질 자동 평가 (`metrics.py`)** — **정답 라벨 없이** 청크 자체만으로 품질 측정
  - **SC**(크기 준수), **BI**(구조 블록 무결성 — 표·제목·코드블록이 안 쪼개졌는지), **ICC**(청크 내부 응집성 — 문장↔청크 임베딩 코사인), **DCC**(문서 맥락 일관성 — 슬라이딩 윈도우와의 정렬), **RC**(대명사-개체 참조 완결성)
- **④ 크기 정규화 (`normalize.py`)**: 과대 청크는 재분할, 과소 청크는 인접 병합해 SC를 안정화

> 문서를 뭉뚱그려 자르지 않고 **타입별로 청킹 전략을 자동으로 갈아끼우고, 잘린 결과를 5개 지표로 스스로 채점**하는 파이프라인이 이 프로젝트 RAG의 핵심입니다.

### ⭐ 2. 청크별 중요도 계산 & HITL 편집 (핵심 차별점 ②)

논문의 청킹 "품질" 지표를 넘어, **청크마다 "얼마나 중요한 내용인가"를 다중 신호로 계산**해 검색·재정렬에 활용하는 스코어링 레이어를 직접 설계했습니다. (논문에 없는 자체 차별화 포인트)

- **다중 신호 중요도 계산 (`scoring.py`)**: 여러 신호를 가중 결합해 청크별 중요도(0~1)를 산출
  - **텍스트 품질**(반복 문자·의미 단어 비율로 노이즈 청크 필터링)
  - **섹션 위치 가중치**(`abstract`·`conclusion`은 가산, `references`·`acknowledgement`는 감산)
  - **키워드 밀도**(doc_type별 사전 — 논문은 `propose/SOTA/향상`, 법령은 `의무/금지/벌칙`, 뉴스는 `발표/보도`)
  - **인용 밀도**(`[1]`, `[2,3]` 등 인라인 인용 카운트, 논문 한정)
  - **LLM 이진 판정**(선택) — "이 청크가 핵심 주장·결론을 담는가"를 물어 `+0.25` 가산
  - 최종 조합: `0.4×키워드 + 0.3×위치 + 0.2×인용 + 0.1×텍스트품질`
- **4단계 라벨링**: 종합 점수를 임계값으로 **Core(≥0.7) / Support(≥0.4) / Context(≥0.15) / Noise** 로 분류
- **품질 × 중요도 결합 → `color_intensity`**: `quality_weight × 품질점수 + importance_weight × 중요도점수`로 청크당 최종 강도(0~1)를 계산해 **Milvus payload에 저장**
- **검색·재정렬에 직접 사용**: 리랭커 API 미가용 시 이 `color_intensity`를 **가중합 폴백**의 한 축으로 사용
- **HITL(Human-in-the-loop) 편집**: 뷰어에서 중요도를 색 강도로 하이라이팅하고, 사용자가 청크를 수정하면 `reembed_and_upsert()`로 **해당 청크만 재임베딩·재적재**(전체 재처리 없이 부분 갱신)

### ⭐ 3. LLM 쿼리 재작성 + 하이브리드 검색 (Vector + BM25 + Graph, RRF Fusion) (핵심 차별점 ③)

검색 정확도를 올리기 위해 **검색 전 LLM으로 사용자 쿼리를 재작성**하고, 서로 다른 세 검색 소스를 순위 기반으로 융합하는 파이프라인을 구현했습니다.

- **LLM 쿼리 재작성 (`query_rewriter.py`)**: 검색 직전 원본 쿼리를 LLM에 넣어 **오타·띄어쓰기 교정 + 핵심 키워드만 남기는 검색 최적화 형태로 재작성**(예: 법령 조문 번호는 `제9조`처럼 붙여쓰기 정규화). **LLM 실패·키 미설정 시 규칙 기반 폴백**(정규식 정규화)으로 항상 동작
- **3-way 병렬 검색**: 재작성된 쿼리로 Vector(Milvus, 시맨틱) + BM25(어휘) + Graph(Neo4j)를 각각 `top_k×3`으로 조회
- **RRF(Reciprocal Rank Fusion, k=60)** 로 순위 기반 병합해 서로 다른 점수 스케일 문제를 해소, **법령 조문 번호 직접 매칭 시 보너스 가산(+0.1)**
- **Rerank**: 외부 Cross-encoder API로 재정렬하고, 미가용 시 위 중요도(`color_intensity`) 기반 **가중합 폴백**(graceful degradation)으로 항상 동작

### 4. 인제스트 파이프라인 오케스트레이션

- **단일 진입점 (`pipeline.py`)**: `문서 타입 분류 → 청킹 전략 채택 → 5지표 품질 평가 → 크기 정규화 → 청크별 중요도 계산 → 임베딩 → Milvus 적재`를 하나의 `run_pipeline()`으로 통합
- **doc_type별 메타데이터화**: 법령은 조문 번호·제목까지 추출해 payload에 저장 → 검색 단계의 조문 직접 매칭 보너스와 연동
- **문서 처리**: `docling`·`PyMuPDF`로 PDF/HTML을 파싱하고 `tiktoken`(cl100k_base)으로 토큰 기준 청크 크기를 통제

### ⭐ 5. Meta-Agent — "에이전트를 만드는 에이전트" (핵심 차별점 ④)

또 하나의 큰 차별점은 **자연어 요구사항을 받아 실행 가능한 에이전트(MCP 도구 Python 코드)를 자동으로 생성·검증·배포**한다는 점입니다. 사용자는 코드를 짜지 않고 "이런 일을 하는 에이전트를 만들어줘"라고 말하면 됩니다.

- **4단계 생성 파이프라인** (LangGraph): `Requirements Analyzer`(요구사항 → 에이전트 명세) → `Tool Retriever`(필요 MCP 도구 검색) → `Provisioner`(실행 가능한 `tool.py` 코드 생성·저장) → `Evaluator`(검증, 실패 시 최대 3회 self-correction)
- **Agent Registry 재사용**: 요구사항 임베딩 유사도(≥ 0.85)로 기존 에이전트를 검색해, 같은 요청이면 **재생성 없이 재사용**(토큰 비용 0)
- **산출물이 실제 파일**: `generated_tools/{agent_id}__{tool_id}/tool.py` + `metadata.json`으로 저장되어 이후 단건 실행·파이프라인 실행·스케줄 등록이 가능

> 단순히 "답변을 잘하는 챗봇"이 아니라, **운영 가능한 소프트웨어(에이전트)를 런타임에 만들어내는 시스템**을 구현한 것도 이 프로젝트의 축입니다.

### 6. Dual Supervisor — 서로를 평가하며 진화하는 쌍방 오케스트레이션

LLM 생성 결과의 품질을 끌어올리기 위해, **두 개의 Supervisor가 서로의 산출물과 평가 기준까지 수정하며 합의에 도달**하는 구조를 설계했습니다. 단순 재시도(retry)와 근본적으로 다릅니다.

- **Builder(A)** 가 에이전트를 생성 → **Critic(B)** 이 평가·개선안 제시 → A가 B의 평가를 **역평가**(기준이 비현실적이면 수정 제안) → 합의 판정 → 배포 (`max_rounds` 가드)
- **실제 실행 기반 자가개선**: Builder가 생성 직후 파이프라인을 **dry-run으로 실제 실행**(메일 등 발송은 건너뜀)해, Critic이 "말이 아닌 실행 결과"를 보고 판정 → 미합의 시 실패 단계를 겨냥해 재생성
- **비용 토글(Critic on/off)**: 프로토타이핑은 **Solo 모드**(1회 생성, 저비용), 프로덕션은 **Dual 모드**(품질 극대화)로 선택

### 7. Research Supervisor — LangGraph 멀티에이전트 리서치

- **Supervisor 패턴**: `StateGraph`로 `supervisor → worker → supervisor` 루프를 구성하고, Supervisor LLM이 **JSON으로 `next_agent`를 라우팅**(search/crawl/graph/analyst/writer 또는 FINISH)
- **5개 워커 에이전트**를 각각 독립 서브그래프로 구현
  - `search`(RAG 검색·충분성 평가), `crawl`(fetch→extract→normalize), `graph`(지식그래프 검색·확장), `analyst`(분석→비평→정제 루프), `writer`(아웃라인→초안→품질평가)
- `max_iteration` 가드로 무한 루프를 차단하고, 상태(`results`)를 누적해 최종 리포트를 생성

### 8. 하네스(Harness) — LLM 노드를 감싸는 자가복구 보호막

Meta-Agent는 여러 LLM 노드가 JSON을 주고받으며 동작하는데, **한 노드의 출력이 JSON으로 안 풀리면 예외가 오케스트레이터까지 전파되어 생성 전체가 HTTP 500으로 죽는** 문제가 있었습니다. 이를 흡수하기 위해 LLM 호출을 감싸는 **하네스**를 직접 구현했습니다.

- **생성 하네스** (`ainvoke_json`): LLM 호출 → JSON 파싱 실패 시 **"깨진 출력 + 복구 지시(유효 JSON으로만 다시 내놔)"** 를 덧붙여 재시도(`retries`), 끝내 실패하면 노드별 **안전한 fallback** 반환 (예: `{"tools": []}`)
- **파서 강화**: `json.loads(..., strict=False)`로 문자열 내부 제어문자를 허용하고, 코드펜스 제거·잘린 JSON 복구 로직을 결합해 파싱 성공률을 높임
- **광범위 적용**: 단발성 유틸이 아니라 `requirements · planner · provisioner · evaluator · builder · critic · counter_review` 등 **핵심 노드 전반과 도구 청소부(tool_janitor)** 에 공통 적용
- **런타임 하네스**(도구 실행 단): 실행 실패를 **전이적 실패(타임아웃·DNS·502/503/504)만 재시도**하고, **코드 버그(NameError 등)와 발송 도구는 재시도하지 않음**(중복 발송·무의미한 반복 차단)

> "생성 단계 JSON 자가복구"와 "실행 단계 전이적 실패 자가복구"를 **별개의 하네스로 분리**해, LLM 파이프라인의 국소 실패가 전체를 무너뜨리지 않도록 설계했습니다. 생성물이 불안정한 LLM 시스템에서 **안정성을 담보하는 핵심 인프라**입니다.

### 9. MCP 도구 런타임 & 스케줄 자동 실행

- **격리 실행**: 생성된 도구를 `subprocess` + JSON-RPC로 실행하고, 인자는 **stdin(JSON)** 으로 전달해 명령행 길이 제한(ARG_MAX)을 회피, `.env`의 LLM/SMTP 키를 도구 환경에 자동 주입
- **스케줄러**: `APScheduler`(Asia/Seoul) cron으로 에이전트를 주기 실행 → `fetch → summarize → send_email` 파이프라인으로 **리포트 메일 자동 발송**, `schedules.json`으로 재시작 시 복원

### 10. 프론트엔드 — 에이전트 운영·시각화 UI

- **ReactFlow + dagre**로 LangGraph 워크플로(Research Supervisor / Dual Supervisor / Meta-Agent 파이프라인)를 **그래프로 시각화**
- **neovis.js**로 Neo4j 문서 지식 그래프를 브라우저에서 직접 렌더링
- 에이전트 생성 · 대시보드(관리/삭제) · 도구 · 스케줄 화면을 REST로 연동

---

## 기술 스택

| 구분 | 기술 |
| --- | --- |
| Backend | Python, FastAPI, Uvicorn, Pydantic Settings |
| Agent / Orchestration | LangGraph(StateGraph), LangChain, MCP(도구 표준), APScheduler |
| LLM | Anthropic Claude(Haiku 등), 멀티 프로바이더 어댑터(Anthropic/OpenAI/Gemini 레지스트리) |
| RAG / 검색 | Milvus Lite(파일 기반), BM25(어휘 검색), RRF Fusion(k=60), 외부 Embedding/Rerank API(Qwen3), 쿼리 재작성 |
| 데이터 전처리 | Adaptive Chunking(문서 타입 분류 → 3종 청커 → 5지표 품질평가 → 크기 정규화), 청크 중요도 스코어링(Core/Support/Context/Noise → `color_intensity`), HITL 부분 재임베딩 |
| Graph | Neo4j 5 (문서 지식 그래프, 조문 직접 매칭 보너스) |
| 문서 처리 | docling, PyMuPDF, tiktoken(cl100k_base 토큰 카운팅), 정규식 구조 경계 분석 |
| Frontend | React 18, Vite 6, TypeScript, React Router 6, ReactFlow, @dagrejs/dagre, neovis.js, axios |
| 알림 / 실행 | subprocess 격리 실행, SMTP(메일 발송) |
| 배포 / 인프라 | Docker Compose(Neo4j), Milvus Lite 로컬 파일, 외부 임베딩/리랭크 서버 |

---

## 트러블슈팅 (기술적 도전 & 해결)

> Meta-Agent는 **LLM이 실행 코드를 직접 생성**하기 때문에, "생성물의 품질 편차"라는 일반 서비스에 없는 고유한 문제를 다뤄야 했습니다. 아래는 반복 실패를 막기 위해 구축한 다층 방어의 핵심입니다.

### 1. LLM 생성 코드의 인자 누락 크래시 (`missing required argument`)

- **문제**: Provisioner가 만든 도구 함수의 시그니처가 파이프라인이 넘기는 인자와 어긋나 실행 즉시 크래시.
- **해결**:
  - **생성 규칙**으로 "모든 함수는 `**kwargs` + 모든 파라미터 기본값"을 강제하고, Evaluator가 위반 시 오류로 판정해 재생성 유도
  - **런타임 방어**로 함수 시그니처를 검사해 **받는 인자만 바인딩**하고, 없는 필수 인자는 빈 값으로 채워 크래시 자체를 차단
- **결과**: LLM 출력이 규칙을 어겨도 실행 단계에서 인자 불일치로 죽지 않음.

### 2. 대용량 페이로드로 인한 실행 실패 (`Errno 7 Argument list too long`)

- **문제**: 크롤링한 원본 HTML을 그대로 다음 도구로 넘기면서, ① 명령행 인자 길이 제한 초과 ② 요약 단계 토큰 초과가 동시에 발생.
- **해결**:
  - fetch/parse 단계에서 원본 HTML 금지 → `get_text()` + **15,000자 절단**, 런타임에서도 전달 텍스트 **50,000자 절단**
  - 도구 인자를 명령행이 아닌 **stdin(JSON)** 으로 전달해 ARG_MAX 제한 우회
- **결과**: 대용량 문서에도 메모리/실행 안정성 확보.

### 3. 발송 도구 누락 & 중복 도구 (Provisioner 결정적 보정)

- **문제**: "메일로 보내줘" 요청인데 LLM이 발송 도구를 빠뜨리거나, 같은 단계 도구를 8개씩 중복 생성.
- **해결**:
  - `_dedupe_tools_by_stage()`로 **단계(fetch/check/summarize/email/schedule)별 1개만 유지**
  - `_ensure_delivery()`로 발송이 필요한데 메일 도구가 없으면 **검증된 표준 SMTP 도구를 결정적으로 주입**(수신자 이메일은 요청문에서 정규식 추출)
  - `_ensure_shared_fetch()`로 깨지기 쉬운 스크레이퍼 대신 **공용 파서 기반 fetch를 주입**
- **결과**: Solo 모드(재생성 루프 없음)에서도 "메일 안 감" 같은 누락을 원천 차단.

### 4. 생성 JSON 파싱 실패로 인한 HTTP 500 (생성 하네스)

- **문제**: Provisioner가 반환하는 JSON의 `"code"` 필드에 **실제 줄바꿈/따옴표**가 들어가 `json.loads`(strict)가 거부 → 노드 예외가 오케스트레이터까지 전파되어 생성 전체가 500으로 실패.
- **해결**:
  - **파서 강화**: `json.loads(..., strict=False)` + 코드펜스·잘린 JSON 복구 로직을 18개 노드에 적용
  - **재시도 래퍼** `ainvoke_json(...)`: 파싱 실패 시 "깨진 출력 + 유효 JSON으로 다시 내놔" 지시를 붙여 재시도하고, 끝내 실패하면 **안전한 fallback** 반환
- **결과**: 한 노드의 출력 오류가 전체 생성을 죽이지 않도록 격리.

### 5. 전이적 실패 vs 코드 버그 구분 재시도 (런타임 하네스)

- **문제**: 에이전트 실행 시 일시적 네트워크 오류까지 실패로 확정하면 잦은 실패, 반대로 무조건 재시도하면 코드 버그를 반복하거나 메일이 중복 발송됨.
- **해결**:
  - `_execute_with_harness`로 실행을 감싸 **타임아웃·DNS·502/503/504 등 전이적 실패만 재시도**
  - **코드 버그(NameError 등)는 재시도 안 함**(같은 입력이면 결과 동일), **발송/알림 도구도 재시도 안 함**(중복 발송 방지)
- **결과**: 불필요한 재시도와 중복 발송을 동시에 차단.

### 6. 삭제 시 공용/공유 도구까지 지워지는 문제 (도구 청소부)

- **문제**: 에이전트를 삭제할 때 전용 도구만 지워야 하는데, 공용 파서나 다른 에이전트가 참조 중인 도구까지 삭제되면 시스템 전체가 깨짐.
- **해결**: `judge_tool_deletions()`를 2단계로 설계 — ① **하드 규칙**(공용 도구·`essential` 표시·타 에이전트 참조 도구는 절대 삭제 금지) → ② **회색지대만 LLM 심판**(delete/keep), 파싱 실패 시 안전한 fallback
- **결과**: 삭제 후 "삭제 N개 / 보호 / 보존" 내역을 프론트에 표시하며 안전한 정리 보장.

### 7. 하이브리드 검색 점수 스케일 불일치 & Rerank 폴백

- **문제**: Vector(의미)·BM25(어휘)·Graph 검색은 점수 스케일이 서로 달라 단순 합산이 불가능하고, 외부 리랭커가 없을 수도 있음.
- **해결**: 세 검색을 **RRF(Reciprocal Rank Fusion)** 로 순위 기반 병합하고, 리랭커 미가용 시 **가중합(검색점수×0.6 + color_intensity×0.4)** 으로 정상 동작(graceful degradation)
- **결과**: 검색 소스가 늘거나 외부 API가 빠져도 견고하게 동작하는 검색 파이프라인.

---

## 주요 성과

- **문서 타입별 청킹 전략을 자동 채택하는 Adaptive Chunking** 파이프라인(3종 청커 + 5지표 무라벨 품질평가 + 크기 정규화) 구축 → 문서 특성에 맞는 분할로 검색 품질 향상
- **청크별 중요도 계산 레이어**(위치·키워드·인용·LLM 판정 다중 신호 → Core/Support/Context/Noise) 설계 → 중요도를 `color_intensity`로 검색·재정렬에 직접 반영, HITL 부분 재임베딩까지 연동
- **LLM 쿼리 재작성 + Vector/BM25/Graph RRF 하이브리드 검색**으로 오타·표현 편차에 강하고 소스가 빠져도 동작하는(graceful degradation) 검색 파이프라인 구축
- **자연어 → 실행 가능한 에이전트(코드) 자동 생성·검증·배포**로 이어지는 Meta-Agent 파이프라인을 구현 → 코딩 없이 자동화 에이전트를 만들고 스케줄로 메일까지 발송하는 E2E 플로우 완성
- **Dual Supervisor 쌍방 진화**(Builder↔Critic + dry-run 실행 기반 자가개선)와 **LangGraph Research Supervisor + 5개 워커 에이전트**로 자율 오케스트레이션 구현
- LLM 생성 코드의 품질 편차를 **생성 규칙 → 런타임 방어 → 자가개선 → 하네스 → 도구 청소부**의 다층 안전망으로 흡수

---

## 회고 & 배운 점

- **RAG 품질은 모델보다 데이터 처리에서 갈린다**는 것을 체감했습니다. 문서 타입별로 청킹 전략을 자동으로 바꾸고, 청크별 중요도를 계산해 검색·재정렬에 반영하고, 검색 전 쿼리를 LLM으로 재작성하는 **전처리·검색 파이프라인 설계**가 답변 품질에 가장 큰 영향을 줬습니다. 특히 정답 라벨 없이 청크 품질을 스스로 채점하는 5지표 설계가 청킹 전략을 정량적으로 비교하는 기준이 되었습니다.
- **"에이전트를 만드는 에이전트"** 를 구현하며, LLM 출력은 신뢰의 대상이 아니라 **다층 방어로 감싸야 하는 입력**이라는 관점을 얻었습니다. 생성 규칙·런타임 가드·하네스를 층층이 쌓는 설계가 결국 시스템 안정성의 핵심이었습니다.
- 멀티에이전트에서 성능을 좌우한 것은 개별 LLM의 성능보다 **오케스트레이션 설계(라우팅·가드·합의 판정)와 실패 격리**였습니다. 특히 "실제 실행 결과로 평가하는" Dual Supervisor 구조가 말뿐인 평가보다 훨씬 유효했습니다.
- LangGraph `StateGraph`로 Supervisor 루프·조건부 엣지·서브그래프를 조립하며, 복잡한 에이전트 흐름을 **상태 기반으로 명시적으로 통제**하는 방법을 체득했습니다.
- 1인 개발로 기획–백엔드–프론트–인프라를 오가며, 기능 단위가 아닌 **시스템 전체 관점에서 우선순위를 판단**하는 역량을 키웠습니다.
```

