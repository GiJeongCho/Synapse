"""SearchAgent 그래프 — 기획 확정 후 LangGraph 구현(§4).

예상 패턴: search → filter → organize → evaluate (self-correction 루프).
"""


def create_search_workflow():
    """검색 에이전트 워크플로우. 기획·노드 확정 후 compile() 반환."""
    raise NotImplementedError("search_agent 그래프 — 기획 확정 후 구현")
