"""WriterAgent 그래프 — 기획 확정 후 구현(§4).

예상 패턴: planner → drafting → evaluation (replanner 루프).
"""


def create_writer_workflow():
    """글쓰기 워크플로우. 기획 확정 후 compile() 반환."""
    raise NotImplementedError("writer_agent 그래프 — 기획 확정 후 구현")
